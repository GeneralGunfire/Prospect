---
phase: 260513-vi3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/community/PotholeMapPage.tsx
  - src/pages/community/FlagPotholePage.tsx
  - src/pages/community/MyContributionsPage.tsx
  - src/services/potholeService.ts
  - src/hooks/usePotholeValidation.ts
  - src/data/saLocations.ts
  - src/utils/suspiciousPatterns.ts
  - src/lib/withAuth.tsx
  - src/App.tsx
  - src/components/shell/AppHeader.tsx
  - src/pages/auth/ImpactAuthPage.tsx
autonomous: true
requirements:
  - REMOVE-POTHOLE-01
  - REMOVE-POTHOLE-02
  - REMOVE-POTHOLE-03

must_haves:
  truths:
    - "No pothole-related files exist in src/pages/community, src/services, src/hooks, src/data (saLocations), src/utils (suspiciousPatterns)"
    - "No Playwright test files matching tests/pothole*.spec.ts exist"
    - "AppPage union type in withAuth.tsx does not contain 'pothole-map', 'flag-pothole', or 'my-pothole-contributions'"
    - "App.tsx contains zero references to PotholeMapPage, FlagPotholePage, or MyContributionsPage (imports and route render blocks)"
    - "No nav/footer/feature links in App.tsx, AppHeader.tsx, ImpactAuthPage.tsx point to pothole-map, flag-pothole, or my-pothole-contributions"
    - "npm run build completes with 0 TypeScript errors"
    - "Kept pages still load: WaterDashboardPage, TaxBudgetPage, CostOfLivingPage, CivicsPage, CommunityImpactPage"
  artifacts:
    - path: "src/lib/withAuth.tsx"
      provides: "AppPage union without pothole entries"
      pattern: "AppPage ="
    - path: "src/App.tsx"
      provides: "Route table without pothole pages"
      pattern: "page === 'water-dashboard'"
    - path: "src/components/shell/AppHeader.tsx"
      provides: "COMMUNITY_NAV without pothole entry"
      pattern: "COMMUNITY_NAV"
  key_links:
    - from: "src/App.tsx Community nav item"
      to: "a non-pothole community page (e.g. water-dashboard or community-impact)"
      via: "navItems array"
      pattern: "name: 'Community'"
    - from: "src/App.tsx footer Community links"
      to: "non-pothole community pages only"
      via: "footer links array"
      pattern: "title: 'Community'"
---

<objective>
Remove the pothole reporting feature from Prospect SA entirely. The product is refocusing on career + education + civic awareness; pothole reporting is out of scope.

Purpose: Eliminate dead-weight feature code, simplify the Community surface area, and ensure a clean build with no broken routes or dangling references.
Output: All pothole source files deleted, all pothole references purged from routing/navigation/auth/header components, build passes cleanly, single commit on the refactor.
</objective>

<execution_context>
@C:/Users/Gener/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/test/landingpage/src/lib/withAuth.tsx
@C:/test/landingpage/src/App.tsx
@C:/test/landingpage/src/components/shell/AppHeader.tsx
@C:/test/landingpage/src/pages/auth/ImpactAuthPage.tsx
@C:/test/landingpage/src/pages/community/CommunityImpactPage.tsx

<interfaces>
<!-- Key locations to modify, extracted via grep. Executor should patch these directly. -->

src/lib/withAuth.tsx line 8 — AppPage union (REMOVE: 'pothole-map' | 'flag-pothole' | 'my-pothole-contributions'):
```typescript
export type AppPage = 'home' | 'auth' | 'dashboard' | 'quiz' | 'subject-selector' | 'library' | 'careers' | 'bursaries' | 'bursary' | 'map' | 'tvet' | 'tvet-careers' | 'tvet-colleges' | 'tvet-funding' | 'tvet-requirements' | 'calendar' | 'school-assist' | 'school-assist-chat' | 'learning-algebra-g10-t1-linear-equations' | 'learning-algebra-g10-t1-simultaneous' | 'pothole-map' | 'flag-pothole' | 'my-pothole-contributions' | 'water-dashboard' | 'tax-budget' | 'cost-of-living' | 'civics' | 'community-impact' | 'impact-auth';
```

src/App.tsx — sites to edit:
- Lines 55-57: lazy imports for PotholeMapPage / FlagPotholePage / MyContributionsPage → DELETE
- Line 86: `{ name: 'Community', page: 'pothole-map' as Page }` → REPLACE page with `'community-impact' as Page` (keep nav item visible, point at remaining community hub)
- Line 211: TUTORIAL_STEPS Community description "Flag potholes, track water outages..." → REPLACE with "Track water outages, explore tax & budget explainers, cost-of-living tools, and civics content."
- Line 307: FeaturesSection Community description "Flag potholes, track water outages..." → REPLACE with same neutral text as above
- Line 309: `page: 'pothole-map' as Page` (Community feature CTA) → REPLACE with `'community-impact' as Page`
- Line 666: Footer `{ label: 'Pothole Map', page: 'pothole-map' }` → DELETE entire entry
- Lines 825-827: route render blocks for 'pothole-map', 'flag-pothole', 'my-pothole-contributions' → DELETE all three lines

src/components/shell/AppHeader.tsx line 40 — COMMUNITY_NAV pothole entry:
```typescript
{ name: 'Pothole Map', page: 'pothole-map', icon: <Construction className="w-3.5 h-3.5" /> },
```
→ DELETE entire line. Also clean up the doc comment on line 20 ("Impact/Potholes/Water/Tax/Civics" → "Impact/Water/Tax/Civics"). Remove the `Construction` import from lucide-react if it's no longer used elsewhere in the file (grep first).

src/pages/auth/ImpactAuthPage.tsx line 42 — features array pothole entry:
```typescript
{ icon: <Users className="w-4 h-4" />, text: 'Pothole Map — report & track road issues' },
```
→ DELETE entire entry. Remove the `Users` import from lucide-react if it's no longer used elsewhere in the file (grep first).

Files to delete outright (use `Remove-Item` on Windows / `rm` on POSIX, NOT just emptied):
- src/pages/community/PotholeMapPage.tsx
- src/pages/community/FlagPotholePage.tsx
- src/pages/community/MyContributionsPage.tsx
- src/services/potholeService.ts
- src/hooks/usePotholeValidation.ts
- src/data/saLocations.ts  (verified only used by pothole files)
- src/utils/suspiciousPatterns.ts  (verified only used by pothole files)

Playwright tests: `Get-ChildItem C:/test/landingpage/tests/pothole*.spec.ts` returned no matches — nothing to delete. Confirm again before completing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete pothole source files and update routing/types</name>
  <files>
    src/pages/community/PotholeMapPage.tsx,
    src/pages/community/FlagPotholePage.tsx,
    src/pages/community/MyContributionsPage.tsx,
    src/services/potholeService.ts,
    src/hooks/usePotholeValidation.ts,
    src/data/saLocations.ts,
    src/utils/suspiciousPatterns.ts,
    src/lib/withAuth.tsx,
    src/App.tsx
  </files>
  <action>
    1. DELETE the seven source files listed above. On Windows PowerShell:
       `Remove-Item C:/test/landingpage/src/pages/community/PotholeMapPage.tsx, C:/test/landingpage/src/pages/community/FlagPotholePage.tsx, C:/test/landingpage/src/pages/community/MyContributionsPage.tsx, C:/test/landingpage/src/services/potholeService.ts, C:/test/landingpage/src/hooks/usePotholeValidation.ts, C:/test/landingpage/src/data/saLocations.ts, C:/test/landingpage/src/utils/suspiciousPatterns.ts`

    2. EDIT `src/lib/withAuth.tsx` line 8: remove `'pothole-map' | 'flag-pothole' | 'my-pothole-contributions' | ` from the AppPage union so it reads `...'learning-algebra-g10-t1-simultaneous' | 'water-dashboard' | ...`.

    3. EDIT `src/App.tsx`:
       - Lines 55-57: delete the three lazy import statements for PotholeMapPage, FlagPotholePage, MyContributionsPage.
       - Line 86: change `{ name: 'Community', page: 'pothole-map' as Page }` → `{ name: 'Community', page: 'community-impact' as Page }`. Rationale: keep Community visible in nav, route it to the remaining hub.
       - Line 211 (TUTORIAL_STEPS): replace `"Flag potholes, track water outages, explore tax & budget explainers, cost-of-living tools, and civics content."` with `"Track water outages, explore tax & budget explainers, cost-of-living tools, and civics content."`
       - Line 307 (FeaturesSection): same string replacement as line 211.
       - Line 309: change `page: 'pothole-map' as Page` → `page: 'community-impact' as Page`.
       - Line 666: delete the entire footer entry `{ label: 'Pothole Map', page: 'pothole-map' },`.
       - Lines 825-827: delete the three route render blocks for `'pothole-map'`, `'flag-pothole'`, `'my-pothole-contributions'`.

    Do NOT touch WaterDashboardPage, TaxBudgetPage, CostOfLivingPage, CivicsPage, CommunityImpactPage, or any of their routes.

    Why these specific edits: deleting the files first means TypeScript will complain about every dangling import/reference on the next build, giving us a safety net. The withAuth/App.tsx edits remove every reference that would cause TS errors.
  </action>
  <verify>
    <automated>cd C:/test/landingpage; npx tsc --noEmit 2>&1 | Select-String -Pattern "error TS" -CaseSensitive | Measure-Object | Select-Object -ExpandProperty Count</automated>
    Expected output: `0`. Also verify no pothole strings remain in modified files:
    `Select-String -Path C:/test/landingpage/src/App.tsx,C:/test/landingpage/src/lib/withAuth.tsx -Pattern "pothole|Pothole"` → no matches.
  </verify>
  <done>
    Seven pothole source files no longer exist on disk. AppPage union has no pothole members. App.tsx has no PotholeMapPage / FlagPotholePage / MyContributionsPage imports, no `'pothole-map'` / `'flag-pothole'` / `'my-pothole-contributions'` route blocks, no footer pothole entry, no nav item pointing at pothole-map. `npx tsc --noEmit` reports 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Purge pothole references from AppHeader and ImpactAuthPage, delete pothole Playwright tests</name>
  <files>
    src/components/shell/AppHeader.tsx,
    src/pages/auth/ImpactAuthPage.tsx
  </files>
  <action>
    1. EDIT `src/components/shell/AppHeader.tsx`:
       - Line 20 doc comment: change `'community' = Impact/Potholes/Water/Tax/Civics` → `'community' = Impact/Water/Tax/Civics`.
       - Line 40: delete the entire COMMUNITY_NAV entry `{ name: 'Pothole Map', page: 'pothole-map', icon: <Construction className="w-3.5 h-3.5" /> },`.
       - Run `Select-String -Path C:/test/landingpage/src/components/shell/AppHeader.tsx -Pattern "Construction"` — if there are zero remaining usages, remove `Construction` from the `lucide-react` import statement at the top of the file. If there are other usages, leave the import alone.

    2. EDIT `src/pages/auth/ImpactAuthPage.tsx`:
       - Line 42: delete the entire features array entry `{ icon: <Users className="w-4 h-4" />, text: 'Pothole Map — report & track road issues' },`.
       - Run `Select-String -Path C:/test/landingpage/src/pages/auth/ImpactAuthPage.tsx -Pattern "<Users"` — if zero remaining usages, remove `Users` from the `lucide-react` import statement. Otherwise leave it.

    3. CONFIRM no Playwright pothole tests exist:
       `Get-ChildItem C:/test/landingpage/tests/ -Filter "pothole*.spec.ts" -ErrorAction SilentlyContinue`
       Expected: empty output (already verified during planning — no matching files). If any do appear, delete them with `Remove-Item`.

    4. Run a final repo-wide sanity grep to ensure no pothole references remain:
       `Select-String -Path C:/test/landingpage/src -Pattern "pothole|Pothole" -Recurse`
       Expected: empty output.

    Avoid touching CommunityImpactPage's internal `'my-contributions'` tab string — that is the CommunityImpactPage's own tab name (a different concept) and is NOT the deleted `'my-pothole-contributions'` route.
  </action>
  <verify>
    <automated>cd C:/test/landingpage; npm run build 2>&1 | Select-String -Pattern "error TS|Build failed|ERROR" | Measure-Object | Select-Object -ExpandProperty Count</automated>
    Expected output: `0`. Also: `Select-String -Path C:/test/landingpage/src -Pattern "pothole|Pothole" -Recurse` returns no matches.
  </verify>
  <done>
    AppHeader.tsx COMMUNITY_NAV has 4 entries (Impact, Water, Tax & Budget, Cost of Living, Civics — i.e. no Pothole Map). ImpactAuthPage features array has no pothole entry. Unused lucide-react imports (Construction, Users) removed where applicable. Repo-wide `pothole|Pothole` grep returns zero matches across `src/` and `tests/`. `npm run build` succeeds with 0 TypeScript errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build verification + commit</name>
  <what-built>
    Pothole feature fully removed: 7 source files deleted, 5 files patched (withAuth.tsx, App.tsx, AppHeader.tsx, ImpactAuthPage.tsx, plus the implicit type-union update), Community nav now points to community-impact, build is green.
  </what-built>
  <how-to-verify>
    1. Run `cd C:/test/landingpage; npm run build` — expect a successful build with 0 TypeScript errors.
    2. Run `cd C:/test/landingpage; npm run dev` and visually confirm:
       - Home page loads, "Community" nav item is visible.
       - Clicking Community → routes to community-impact (Opportunities page), NOT a broken route.
       - Footer Community section lists Water Dashboard, Tax & Budget, Cost of Living, Civics — no "Pothole Map" link.
       - Kept pages still work: /water-dashboard, /tax-budget, /cost-of-living, /civics, /community-impact.
    3. (If Playwright is available) Run `npx playwright test --reporter=line` and confirm no test failures introduced by this change (per project memory: always run Playwright before commit).
    4. Once verified, commit with:
       `git add -A; git commit -m "refactor: remove pothole feature, focus on career+education+civic awareness"`
  </how-to-verify>
  <resume-signal>Type "approved" to confirm build/visual check passed and commit was made, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with no TypeScript errors.
- `Select-String -Path C:/test/landingpage/src -Pattern "pothole|Pothole" -Recurse` returns empty.
- `Select-String -Path C:/test/landingpage/tests -Pattern "pothole|Pothole" -Recurse` returns empty.
- All seven target files no longer exist on disk.
- AppPage union in `src/lib/withAuth.tsx` contains no pothole members.
- Community nav item in App.tsx points to `'community-impact'`, not `'pothole-map'`.
- Footer Community section in App.tsx has no Pothole Map entry.
- AppHeader.tsx COMMUNITY_NAV has no Pothole Map entry.
- ImpactAuthPage features array has no Pothole Map entry.
- Kept pages (WaterDashboardPage, TaxBudgetPage, CostOfLivingPage, CivicsPage, CommunityImpactPage) still render in dev server.
- Playwright (if run) passes.
- Single commit on disk with message `refactor: remove pothole feature, focus on career+education+civic awareness`.
</verification>

<success_criteria>
Plan is complete when:
1. Build is green (`npm run build` → 0 TS errors).
2. Zero pothole references remain in `src/` or `tests/` (grep proof).
3. Community surface area still functions — the Community nav item routes to the community-impact hub and the kept community pages remain reachable.
4. A single commit with the exact specified message captures all changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260513-vi3-remove-pothole-feature-focus-on-career-e/260513-vi3-01-SUMMARY.md` documenting:
- Files deleted (7 sources + any Playwright tests if found)
- Files patched (withAuth.tsx, App.tsx, AppHeader.tsx, ImpactAuthPage.tsx)
- Any incidental cleanup (unused lucide-react imports removed)
- Build result and commit hash
</output>
